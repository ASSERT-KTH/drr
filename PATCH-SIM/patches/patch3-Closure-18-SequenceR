diff -w -r -u Closure18b/src/com/google/javascript/jscomp/Compiler.java Closure18b_patch3-Closure-18-SequenceR/src/com/google/javascript/jscomp/Compiler.java
--- Closure18b/src/com/google/javascript/jscomp/Compiler.java
+++ Closure18b_patch3-Closure-18-SequenceR/src/com/google/javascript/jscomp/Compiler.java
@@ -1285,7 +1285,7 @@
 
       // Check if the sources need to be re-ordered.
       boolean staleInputs = false;
-      if (options.dependencyOptions.needsManagement() && options.closurePass) {
+      if(((options)!=null)&&(options.dependencyOptions.needsManagement())){
         for (CompilerInput input : inputs) {
           // Forward-declare all the provided types, so that they
           // are not flagged even if they are dropped from the process.

