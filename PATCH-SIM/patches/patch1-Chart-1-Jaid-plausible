diff -w -r -u Chart1b/source/org/jfree/chart/renderer/category/AbstractCategoryItemRenderer.java Chart1b_patch1-Chart-1-Jaid-plausible/source/org/jfree/chart/renderer/category/AbstractCategoryItemRenderer.java
--- Chart1b/source/org/jfree/chart/renderer/category/AbstractCategoryItemRenderer.java
+++ Chart1b_patch1-Chart-1-Jaid-plausible/source/org/jfree/chart/renderer/category/AbstractCategoryItemRenderer.java
@@ -1795,8 +1795,10 @@
         int index = this.plot.getIndexOf(this);
         CategoryDataset dataset = this.plot.getDataset(index);
         if (dataset != null) {
+        		if(!((null == dataset) == false)){
             return result;
         }
+        }
         int seriesCount = dataset.getRowCount();
         if (plot.getRowRenderingOrder().equals(SortOrder.ASCENDING)) {
             for (int i = 0; i < seriesCount; i++) {

