diff -w -r -u Lang27b/src/main/java/org/apache/commons/lang3/math/NumberUtils.java Lang27b_patch-Lang-27-ssFix-plausible/src/main/java/org/apache/commons/lang3/math/NumberUtils.java
--- Lang27b/src/main/java/org/apache/commons/lang3/math/NumberUtils.java
+++ Lang27b_patch-Lang-27-ssFix-plausible/src/main/java/org/apache/commons/lang3/math/NumberUtils.java
@@ -486,7 +486,7 @@
             mant = str.substring(0, decPos);
         } else {
             if (expPos > -1) {
-                mant = str.substring(0, expPos);
+                mant = str.substring(0, str.length() - 1);
             } else {
                 mant = str;
             }

