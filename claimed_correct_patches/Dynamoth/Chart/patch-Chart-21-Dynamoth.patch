--- /source/org/jfree/data/Range.java
+++ /source/org/jfree/data/Range.java
@@ -334,3 +334,5 @@
         if (!(this.lower == range.lower)) {
-            return false;
+            if (range.getLowerBound() == range.getLength()) {
+                return false;
+            }
         }