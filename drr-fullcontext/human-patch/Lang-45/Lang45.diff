--- ./tmp/Lang45buggy/src/java/org/apache/commons/lang/WordUtils.java	2019-05-05 07:57:49.000000000 -0700
+++ ./tmp/Lang45fix/src/java/org/apache/commons/lang/WordUtils.java	2019-05-05 07:57:55.000000000 -0700
@@ -613,6 +613,9 @@
 
         // if the lower value is greater than the length of the string,
         // set to the length of the string
+        if (lower > str.length()) {
+            lower = str.length();    
+        }
         // if the upper value is -1 (i.e. no limit) or is greater
         // than the length of the string, set to the length of the string
         if (upper == -1 || upper > str.length()) {
